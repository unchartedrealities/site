import { WorldsPipePipe } from './worlds-pipe.pipe';

describe('WorldsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new WorldsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
